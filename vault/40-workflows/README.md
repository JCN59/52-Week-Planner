# Workflows

Repeatable how-to's. If something gets done twice, it becomes a workflow note:
trigger → steps → tools involved → gotchas. Agents: follow the workflow note when
one exists; propose edits when reality has drifted from it.

Starter candidates (write these up as they get used):
- Weekly Meta ads review (Scrum Doc update → 4Pi analysis → optimization calls)
- Design-intel research run → design concepts → Shopify products
- Vault upkeep: file the inbox, prune TASKS.md, log decisions
