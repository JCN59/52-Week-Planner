# Projects

One note per active project, from `60-templates/project.md`. For software, the
code lives in its repo — this folder holds the *context*: goal, status, decisions,
next steps. Archive finished projects by prefixing the filename with `zz-archived-`.
