# Decisions

Dated log of decisions, newest first. One or two lines each: what was decided and why.

- **2026-07-28** — Created the vault inside the `52-Week-Planner` repo (branch
  `claude/personal-ai-vault-obsidian-dvc59z`) for a fast start; plan is to migrate
  it to its own private repo (see `README.md` → Migration plan).
- **2026-07-28** — One rulebook for all tools: `AGENTS.md` is the source of truth,
  `CLAUDE.md` just points to it.
